{
  "name": "@akoris/connectors",
  "version": "1.0.0",
  "description": "AKORIS Connectors - Integration with external services",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@akoris/sdk": "workspace:*",
    "typescript": "^5.5.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
